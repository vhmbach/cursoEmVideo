# len() - Exibe a quantidade de caracteres
# count('') - exibe quantas vezes o caractere especificado aparece na linha
# find() - realiza uma busca na linha pelos caracteres specificados
# replace() - substitui um conjunto de caracteres por outro especificado
# upper() - converte todos os caracteres para letras maiusculas
# lower() - substitui todos os caracteres para letras minusculas
# captalize() - converte o primeiro caractere para maiusculo e o restante minusculo
# title() - converte o primeiro caractere de cada palavra para maiusculo e o restante minusculo
# strip() - converte as palavras de uma
# join () - ao contrario do strip, o join realiza a junção das palavras

